import { CoolButtonDirective } from './cool-button.directive';

describe('CoolButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CoolButtonDirective();
    expect(directive).toBeTruthy();
  });
});
